import { encodeMemory } from "@/api/config/model"

let section16 = []
let section18 = []
let section20 = []
let data = 
[].concat(section16)
    .concat(section18)
    .concat(section20)


export{
    data
}