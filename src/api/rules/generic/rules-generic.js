import { eng } from "./eng"
import {fil} from "./fil"
import {mag} from "./mag"




export const genericRules = 
    [].concat(eng)
        .concat(fil)
        .concat(mag)
        // .concat(engSection16to20)
