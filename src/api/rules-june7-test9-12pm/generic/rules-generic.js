import { eng } from "./eng"
import {fil} from "./engfil"
import {mag} from "./engmag"




export const genericRules = 
    [].concat(eng)
        .concat(fil)
        .concat(mag)
        // .concat(engSection16to20)
